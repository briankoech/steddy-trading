interface IService {
  link: string;
  img: string;
  description: string;
  title: string;
  featured?: boolean;
}

interface IProject {
  link: string;
  img: string;
  title: string;
  featured?: boolean;
}
